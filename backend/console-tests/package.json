{
  "name": "console-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for the owner-console ES modules (DIYHelper2.Api/wwwroot/admin/js). Lives outside wwwroot so node_modules is never served.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.1.4"
  }
}
